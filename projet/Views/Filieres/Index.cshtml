@model IEnumerable<projet.Models.Filiere>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nom_fil)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nom_fil)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_fil">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_fil">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_fil">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
